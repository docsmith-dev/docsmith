{
  "name": "docsmith",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "clean": "pnpm -r clean",
    "core": "pnpm --filter '@docsmith/core'",
    "react": "pnpm --filter '@docsmith/react'",
    "examples:react": "pnpm --filter './examples/react-app'"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "typescript": "^5.3.3"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  }
}